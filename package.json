{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node bin/www"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "mongoose": "^6.3.0",
    "socks": "^2.6.2"
  }
}
